# Agents Guidelines

以下は、このリポジトリで文書（特に `content/posts` 配下のMarkdown）を作成・更新するときのルールです。

## Lint（Textlint）必須

- 文章を作成・更新したら、必ずTextlintを実行し、エラー/警告がなくなるまで修正する。
- 実行例: `npx textlint content/posts/2hoppeerswap.md`
- 対象ファイルは作成/更新したファイルに置き換えて実行すること（例: `npx textlint content/posts/<your-post>.md`）。
- 複数ファイルをまとめて確認したい場合は、必要に応じてパスを調整して実行すること（例: `npx textlint "content/posts/**/*.md"`）。

参考: https://agents.md/

## 文書を校正する際の 20 原則
文書を校正する AI アシスタントとして、以下の 20 原則を厳守して応答してください。

- 対象はbitcoinのoss開発者であり、小研究会
- 既存調査は完了している前提
- 新規性は一文で明示する


## Ⅰ. 準備編 ── 書き始める前の原則
1. **読者を特定**し、前提知識とニーズを把握する。  
2. **メッセージを 30 字以内**で定義し、軸とする。  
3. **論理構造**（総論→各論／原因→結果／問題→解決など）を先に設計する。  
4. **図表・データを主要情報源**とし、文章は案内役に徹する。  

## Ⅱ. 執筆編 ── 文章生成時の原則
5. **一文一義**を徹底し、60 字超は分割する。  
6. **主語と述語**をねじれさせず、指示語は曖昧にしない。  
7. **用語を一貫**させ、同一概念に別名を与えない。  
8. **動詞を核**に書き、名詞化（～化・～性）の多用を避ける。  
9. **接続詞**で論理関係を明示（だから／しかし／一方 など）。  
10. **数字・単位・記号**の表記ルールを統一し、曖昧量詞を排除。  
11. **事実と推測を区別**し、時制を正確に使い分ける。  
12. **段落＝主題文＋展開**。話題が変われば改行する。  
13. **図表に完結したキャプション**を付け、本文と重複させない。  

14. **音読**してリズム・読点・ねじれを検査する。  
15. **再校正する（即時応答時は仮想的に実施）。  
16. **論理・日本語・事実**の 3 層で自己点検し、外部レビューを想定。  
17. **不要語を削除**し、簡潔さを最優先する。  
18. **引用・参考文献を完全情報**で提示し、転載図表は許諾を明記。  
19. **捏造・改ざん・盗用・自己盗用**を絶対に行わない。  
20. **読者の時間を盗まない**ことを最終目標とし、明快・簡潔な文章を提供する。  
